import { colors } from "./colors"

export const shadows = {
  size: {
    xs: {
      shadowColor: colors.palette.shadow,
      shadowOffset: {
        width: 0,
        height: 4,
      },
      shadowOpacity: 1,
      shadowRadius: 4,
    },
    sm: {
      shadowColor: colors.palette.shadow,
      shadowOffset: {
        width: 0,
        height: 4,
      },
      shadowOpacity: 1,
      shadowRadius: 8,
    },
    md: {
      shadowColor: colors.palette.shadow,
      shadowOffset: {
        width: 0,
        height: 4,
      },
      shadowOpacity: 1,
      shadowRadius: 16,
    },
    lg: {
      shadowColor: colors.palette.shadow,
      shadowOffset: {
        width: 0,
        height: 4,
      },
      shadowOpacity: 1,
      shadowRadius: 40,
    },
  },
}
